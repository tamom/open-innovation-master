{% extends 'template.html.twig' %}
{% block title %}Etudiant{% endblock %}

{% block body %}
    <h1>Etudiant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etudiant.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ etudiant.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ etudiant.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ etudiant.email }}</td>
            </tr>
            <tr>
                <th>Classe</th>
                <td>{{ etudiant.classe }}</td>
            </tr>
            <tr>
                <th>Absence</th>
                <td>{{ etudiant.absence }}</td>
            </tr>
            <tr>
                <th>Retard</th>
                <td>{{ etudiant.retard }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('etudiant_index') }}" class="btn btn-primary">Retourner à la liste</a>

    <a href="{{ path('etudiant_edit', {'id': etudiant.id}) }}" class="btn btn-warning" style="margin-left:10%">Modifier</a>
<div style="margin-top:20px">
    {{ include('etudiant/_delete_form.html.twig') }}
</div>
{% endblock %}
